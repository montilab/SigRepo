% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypeRClient.R
\name{runHypeR}
\alias{runHypeR}
\title{Run hypeR enrichment on SigRepo signatures}
\usage{
runHypeR(
  conn_handler = NULL,
  signature_id = NULL,
  signature_name = NULL,
  omic_signature = NULL,
  genesets,
  msigdb_species = NULL,
  msigdb_collection = NULL,
  msigdb_subcollection = NULL,
  msigdb_clean = FALSE,
  test = c("hypergeometric", "kstest"),
  split = TRUE,
  score_col = "score",
  background = 23467,
  power = 1,
  absolute = FALSE,
  pval = 1,
  fdr = 1,
  plotting = FALSE,
  quiet = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{conn_handler}{An R object obtained from \code{SigRepo::newConnHandler()}.
Needed to fetch signatures by id/name and for reference-table symbol lookup.}

\item{signature_id}{One or more SigRepo signature IDs.}

\item{signature_name}{One or more SigRepo signature names.}

\item{omic_signature}{An \code{OmicSignature} object or a list of them. List
names, when given, become the query labels.}

\item{genesets}{Either the string \code{"msigdb"}, or your own genesets: a
named list of character vectors, a \code{hypeR::gsets} object, or a
\code{hypeR::rgsets} object.}

\item{msigdb_species}{Species for MSigDB, e.g. \code{"Homo sapiens"} or
\code{"Mus musculus"}. Defaults to \code{"Homo sapiens"}. Only valid with
\code{genesets = "msigdb"}.}

\item{msigdb_collection}{MSigDB collection, e.g. \code{"H"}, \code{"C2"},
\code{"MH"}. Required with \code{genesets = "msigdb"}.}

\item{msigdb_subcollection}{Optional MSigDB subcollection, e.g.
\code{"CP:REACTOME"}. Only valid with \code{genesets = "msigdb"}.}

\item{msigdb_clean}{Logical; clean geneset labels via
\code{hypeR::gsets$new(clean = TRUE)}. Defaults to \code{FALSE}.}

\item{test}{\code{"hypergeometric"} (a character vector of symbols from the
signature table) or \code{"kstest"} (a named numeric vector ranked by
\code{score_col} from the full difexp table).}

\item{split}{Logical; for \code{"hypergeometric"}, build one vector per
\code{group_label} (e.g. one per arm of a bi-directional signature). Ignored by
\code{"kstest"}, whose ranked list already carries direction in its sign.
Defaults to \code{TRUE}.}

\item{score_col}{difexp column used to rank genes for \code{"kstest"}.
Defaults to \code{"score"}.}

\item{background}{hypeR's background: a single number (population size), a
character vector of background genes, or \code{"difexp"} to use each
signature's measured genes from its difexp table. Signatures without difexp
symbols fall back to \code{23467} with a warning. Defaults to \code{23467}.}

\item{power}{Exponent for score weights (kstest only). \code{1} (default)
weights hits by score, GSEA-style; \code{0} is the classic unweighted KS
statistic.}

\item{absolute}{Passed to \code{hypeR::hypeR()} (kstest only). Defaults to \code{FALSE}.}

\item{pval}{Keep results with p-value at or below this. Defaults to \code{1}.}

\item{fdr}{Keep results with FDR at or below this. Defaults to \code{1}.}

\item{plotting}{Logical; generate hypeR's per-geneset plots. Defaults to \code{FALSE}.}

\item{quiet}{Logical; suppress hypeR's logs. Defaults to \code{TRUE}.}

\item{verbose}{Logical; print messages while fetching signatures.}
}
\value{
A \code{hypeR} \code{hyp} object when one query vector is produced,
otherwise a \code{multihyp} named by query. Each \code{hyp$info} ends with
\code{SigRepo Signature ID}, \code{SigRepo Signature Name},
\code{Group Label} and \code{Symbol Source}. Signatures that cannot produce a
query are skipped with a warning; if none can, an error is raised.
}
\description{
Builds hypeR query vectors from SigRepo signatures (see
\code{prepareHypeRSignatures()}) and runs \code{hypeR::hypeR()} on them. The
arguments after \code{score_col} are hypeR's own, with hypeR's defaults, and
the result is hypeR's own object, so \code{hypeR::hyp_dots()},
\code{hyp_show()}, \code{hyp_emap()}, \code{hyp_to_excel()} and
\code{hyp_to_rmd()} work on it directly.
}
\examples{
\dontrun{
utils::data("LLFS_Aging_Gene_2023", package = "SigRepo")

hyp <- SigRepo::runHypeR(
  omic_signature = LLFS_Aging_Gene_2023,
  genesets = "msigdb",
  msigdb_collection = "H",
  fdr = 0.05
)
hypeR::hyp_dots(hyp)

ranked <- SigRepo::runHypeR(
  omic_signature = LLFS_Aging_Gene_2023,
  genesets = "msigdb",
  msigdb_collection = "H",
  test = "kstest",
  power = 0
)
}

}
