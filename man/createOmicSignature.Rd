% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createOmicSignature.R
\name{createOmicSignature}
\alias{createOmicSignature}
\title{createOmicSignature}
\usage{
createOmicSignature(
  conn_handler = NULL,
  db_signature_tbl,
  difexp = NULL,
  fetch_difexp = TRUE
)
}
\arguments{
\item{conn_handler}{An R object obtained from SigRepo::newConnhandler() (required)}

\item{db_signature_tbl}{A Data Frame; must contain the following column names:
'signature_id'}

\item{difexp}{Optional pre-loaded difexp data frame. Only used when
\code{fetch_difexp = FALSE}; lets a caller that already has the difexp on
hand (e.g. the SigRepo API, reading it from disk) inject it instead of
having createOmicSignature fetch it over HTTP.}

\item{fetch_difexp}{Logical; when \code{TRUE} (default, the original
behavior) difexp is retrieved from the SigRepo API's \code{get_difexp}
endpoint. Set \code{FALSE} to skip that HTTP round-trip and use the
\code{difexp} argument as-is -- required when calling this from inside the
API process itself, where an HTTP call back to the same single-process
server would deadlock.}
}
\description{
Get the signature set uploaded by a specific user in the database.
}
\keyword{internal}
