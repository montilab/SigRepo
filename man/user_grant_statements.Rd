% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/userGrants.R
\name{user_grant_statements}
\alias{user_grant_statements}
\title{user_grant_statements}
\usage{
user_grant_statements(user_name, user_role, dbname)
}
\arguments{
\item{user_name}{MySQL account name (one string)}

\item{user_role}{One of "admin", "editor", "viewer". Anything else yields no statements.}

\item{dbname}{Name of the database the grants apply to, normally
`DBI::dbGetInfo(conn)$dbname` of the connection issuing them.}
}
\value{
A character vector of SQL statements, possibly empty.
}
\description{
Build the GRANT statements that give a SigRepo account the
MySQL privileges its role needs. The schema is whatever database the
connection is on, never a literal name: a hard-coded `sigrepo` here made
`addUser()` fail with "Table 'sigrepo.users' doesn't exist" on any database
with another name, because MySQL requires the table to exist for a
table-level grant (#245, montilab/SigRepo_Server#130).
}
\keyword{internal}
