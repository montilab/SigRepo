# conn_init

Initiate a remote database connection

## Usage

``` r
conn_init(conn_handler)
```

## Arguments

- conn_handler:

  A handler uses to establish connection to a remote database obtained
  from SigRepo::newConnHandler() (required)

## Value

a MySQL connection class object.
