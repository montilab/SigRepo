# getNumOfObs

Check api key whether it is valid to access the database

## Usage

``` r
getNumOfObs(conn, db_table_name)
```

## Arguments

- conn:

  An established connection to database using SigRepo::newConnhandler()

- db_table_name:

  A table in the database
