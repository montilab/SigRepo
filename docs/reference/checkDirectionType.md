# checkDirectionType

Check if direction type is a valid type in the database

## Usage

``` r
checkDirectionType(direction_type)
```

## Arguments

- direction_type:

  A direction type of the signature
