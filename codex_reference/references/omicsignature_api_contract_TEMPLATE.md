# OmicSignature API contract summary

This document will be completed after running:

```bash
Rscript codex_reference/tests/test_omicsignature_contract.R
```

Record:

- test date;
- R version;
- OmicSignature version;
- installed Git commit when available;
- resolved sample/platform search functions;
- metadata scalar behavior;
- automatic probe-ID behavior;
- repeated-feature preservation;
- collection member access;
- final pass/fail status; and
- any compatibility-layer or package changes required.

The generated result files are diagnostic evidence. This Markdown file is the
human-reviewed contract summary that will later be included in the Skill.
